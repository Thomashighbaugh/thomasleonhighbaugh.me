- TODO Fix the rendering ON MOBILE VIEWPORTS ONLY of the introduction page's text (my name). It should appear without as much gap between the the SVG animated image and the text, the text being centered still (on my Pixel 10 Pro this renders off screen and left aligned). 
- TODO Convert the fonts in public/ to embedded fonts in the SVGs themselves, in the project's CSS and then eliminate the need for the font files in public in general. (neater project directories at the expense of creating text blobs).
- TODO Blog post - outline, draft, critique & revise a part 2 to the recent blog post my opencode configuration featuring the custom Hubs plugin. In this part the discussion should be about the configuration's use of durable, sharable context in .opencode/context, which enables the saving of session information synthesized into a durable and applicable set of lessons, relevant details about the codebase, ingested context from local files, urls or context7's MCP to create a local, project specific knowledge bases in the project. This is contrasted by the artifacts of the operation of opencode, which is state and includes things like session logs, which could compromise security (therefore are added to a project's gitignore at initialization), plans that were presented to the user, work products etc. 
- TODO 
- TODO 
- TODO 

